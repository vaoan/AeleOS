---
paths:
  - "apps/hub/src/features/actors/presentation/**"
---

# Editor presentation

Loaded when a presentation component under the actors feature's editor is read — the renderers, the editor chrome, the leaf modules, the style popups. Each line is the rule; the full account of every line is in the feature's `HISTORY.md` beside `CLAUDE.md`.

- **The identity leaves (2026-08-19) — content that is not typed** — `avatar`, `handle`, `name`, `owner` and `fursonas` draw the ACTOR, resolved out of `PageContext`, never what an author typed. → `apps/hub/src/features/actors/HISTORY.md`, section "The identity leaves (2026-08-19) — content that is not typed"
- **At least one of each — and what that does NOT guarantee** — The guarantee is that the block EXISTS IN THE TREE, not that a visitor sees it, and that hole is accepted rather than overlooked. → `apps/hub/src/features/actors/HISTORY.md`, section "At least one of each — and what that does NOT guarantee"
- **A container is its SPACES, and there are no spans** — `columns` and `span` are gone and nothing replaced them. → `apps/hub/src/features/actors/HISTORY.md`, section "A container is its SPACES, and there are no spans"
- **Adding a mode or a kind — what is guarded, and what is not** — Those lookups are `Map`s rather than records because of a fault, not a style rule. → `apps/hub/src/features/actors/HISTORY.md`, section "Adding a mode or a kind — what is guarded, and what is not"
- **What a template ships, and what a title and a description mean** — A template ships structure, never prose. → `apps/hub/src/features/actors/HISTORY.md`, section "What a template ships, and what a title and a description mean"
- **The editor composes blocks, and the shim converts what is already stored** — The whole page is ONE form field, held by one `useController`, and that is forced rather than preferred. → `apps/hub/src/features/actors/HISTORY.md`, section "The editor composes blocks, and the shim converts what is already stored"
- **Telling a section from content (2026-08-27)** — Three channels separate the two kinds, and none of them is colour alone: a rail down one edge against a perimeter, `--accent` against `--edge`, and thin against thick. → `apps/hub/src/features/actors/HISTORY.md`, section "Telling a section from content (2026-08-27)"
- **Dragging (2026-08-18; inspector corrected 2026-09-01)** — The two halves of a drag live in `domain/`, not in a component. → `apps/hub/src/features/actors/HISTORY.md`, section "Dragging (2026-08-18; inspector corrected 2026-09-01)"
- **A frame is the height its provider actually paints (2026-08-19)** — `FRAME_SHAPE` used to decide a height and no longer does. → `apps/hub/src/features/actors/HISTORY.md`, section "A frame is the height its provider actually paints (2026-08-19)"
- **Era looks (2026-08-28) — five OPTIONS, and not one new skin** — Each look wears its release's own mark as an avatar, and is public and listed (2026-08-29). → `apps/hub/src/features/actors/HISTORY.md`, section "Era looks (2026-08-28) — five OPTIONS, and not one new skin"
- **A template is a document too (2026-08-28)** — `applyDocumentTo` in `fursona-editor.tsx` is the one path that applies both a pasted document and a picked template — never add a second implementation. → `apps/hub/src/features/actors/HISTORY.md`, section "A template is a document too (2026-08-28)"
- **A page has a document (2026-08-27)** — A `theme` that is present but not a plain object — `[]`, a string, a number — is refused as its own `envelope` problem too (2026-08-28), found by a whole-branch review. → `apps/hub/src/features/actors/HISTORY.md`, section "A page has a document (2026-08-27)"
- **The document is bound to the page live, in both directions (2026-08-28)** — The page holds the last good tree because a bad parse never writes anything, not because a copy is kept anywhere. → `apps/hub/src/features/actors/HISTORY.md`, section "The document is bound to the page live, in both directions (2026-08-28)"
- **Per-profile theming — built** — A theme is ONE palette, not a light and a dark variant. → `apps/hub/src/features/actors/HISTORY.md`, section "Per-profile theming — built"
- **How a visitor gets out** — A page wears its owner's colours by default, and `PageThemeSwitch` is the way out. → `apps/hub/src/features/actors/HISTORY.md`, section "How a visitor gets out"
- **Skins — the half of a theme that is not colour** — What earns a place is a MECHANISM, not another set of numbers. → `apps/hub/src/features/actors/HISTORY.md`, section "Skins — the half of a theme that is not colour"
- **What `cutout` cost, which is the part worth reading before the next skin** — `clip-path` clips the element's whole subtree, positioned descendants included. → `apps/hub/src/features/actors/HISTORY.md`, section "What `cutout` cost, which is the part worth reading before the next skin"
- **A block's own form (2026-08-16)** — Every key is optional, and absent means the existing/default answer rather than an empty value. → `apps/hub/src/features/actors/HISTORY.md`, section "A block's own form (2026-08-16)"
- **`border` (2026-08-16), and the token it deliberately is not** — A choice also raises a FLOOR under the border's width, `--skin-border-min`, and without it most of the choices did nothing. → `apps/hub/src/features/actors/HISTORY.md`, section "`border` (2026-08-16), and the token it deliberately is not"
- **The section preview's face is not the control card** — `card_size` is in the schema, in no popup, and read by no page. → `apps/hub/src/features/actors/HISTORY.md`, section "The section preview's face is not the control card"
- **The nesting fix, and why `skinVars` was left alone** — `blockStyle` calls `nestedSkinVars`, never plain `skinVars`, when nesting — widening `skinVars` itself would break the untouched-page-emits-nothing invariant. → `apps/hub/src/features/actors/HISTORY.md`, section "The nesting fix, and why `skinVars` was left alone"
- **The preview and the public page share one function** — `blockStyle` (`presentation/block-style.ts`) is the only place this renders. → `apps/hub/src/features/actors/HISTORY.md`, section "The preview and the public page share one function"
- **Readability keeps no per-block escape hatch** — It needs no per-block way out, and none should be added. → `apps/hub/src/features/actors/HISTORY.md`, section "Readability keeps no per-block escape hatch"
- **The page's own background picture (Phase D)** — It renders as a second `background-image` layer on `body`, above `var(--field)` — never at `:root`, because `body` paints its own opaque background over `:root`'s. → `apps/hub/src/features/actors/HISTORY.md`, section "The page's own background picture (Phase D)"
- **Canvases** — `CANVASES` holds exactly the canvases that exist; a canvas joins the list in the same change that implements it, or it falls through to the nebula silently. → `apps/hub/src/features/actors/HISTORY.md`, section "Canvases"
- **How wide a page is, and a section that ignores its chrome (2026-08-21)** — An enum rather than a number, and the reason is mechanical. → `apps/hub/src/features/actors/HISTORY.md`, section "How wide a page is, and a section that ignores its chrome (2026-08-21)"
- **The theme switch is in the bar (2026-08-19)** — The light/dark toggle clears `data-page-theme` as well as setting `data-theme`, so the press always changes something a visitor can see. → `apps/hub/src/features/actors/HISTORY.md`, section "The theme switch is in the bar (2026-08-19)"
- **The writing switch is in the bar too (2026-08-28)** — The language strip — `writingIn`, "Writing in" — is gone. → `apps/hub/src/features/actors/HISTORY.md`, section "The writing switch is in the bar too (2026-08-28)"
- **The architecture pass (2026-08-27) — what moved and what deliberately did not** — CSS emission lives in `presentation/theme-css.ts`, never in `domain/`; `domain/actor-theme.ts` keeps only the vocabulary. → `apps/hub/src/features/actors/HISTORY.md`, section "The architecture pass (2026-08-27) — what moved and what deliberately did not"
- **`list` — the divided list (2026-08-27)** — It is an ARRANGEMENT and decides nothing about its children. → `apps/hub/src/features/actors/HISTORY.md`, section "`list` — the divided list (2026-08-27)"
- **Closing the pastiche gaps (2026-08-27) — five new OPTIONS** — Three keys on a block's style bag — `chrome` (`card`/`bare`), `heading` (`plain`/`bar`/`gradient`), `text_align` — plus `font` and `spacing` on the page theme; absence must mean exactly what the page did before the key existed. → `apps/hub/src/features/actors/HISTORY.md`, section "Closing the pastiche gaps (2026-08-27) — five new OPTIONS"
- **Closing the REST of the pastiche gaps (2026-08-27) — four more options** — `image_fit`'s default lives at `:root`; absence emits NOTHING rather than `cover`, so an unstyled block never overwrites an enclosing section's `contain`. → `apps/hub/src/features/actors/HISTORY.md`, section "Closing the REST of the pastiche gaps (2026-08-27) — four more options"
- **A bar can be given a QUIETER tone (2026-08-28)** — The tone is DERIVED, never picked, and that is the whole design. → `apps/hub/src/features/actors/HISTORY.md`, section "A bar can be given a QUIETER tone (2026-08-28)"
- **A bar can be given room (2026-08-28)** — `heading_pad` — `snug` or `roomy`, absent being the `px-3 py-2` every barred page already had. → `apps/hub/src/features/actors/HISTORY.md`, section "A bar can be given room (2026-08-28)"
- **A title bar is a design surface (2026-08-29)** — `heading_image` paints a picture ON the bar, over the fill rather than instead of it. → `apps/hub/src/features/actors/HISTORY.md`, section "A title bar is a design surface (2026-08-29)"
- **The window shape: corners chosen one at a time (2026-08-29)** — It writes TOKENS rather than `border-radius`, and a browser is what forced that. → `apps/hub/src/features/actors/HISTORY.md`, section "The window shape: corners chosen one at a time (2026-08-29)"
- **A block may hide its own title as a label (2026-08-29)** — `hidden` NARROWS what the enclosing mode already decided; it never WIDENS it. → `apps/hub/src/features/actors/HISTORY.md`, section "A block may hide its own title as a label (2026-08-29)"
- **A leaf reaches its own style popup (2026-08-30)** — Generalised from two ad-hoc booleans to one computed value, rather than adding a third boolean beside them. → `apps/hub/src/features/actors/HISTORY.md`, section "A leaf reaches its own style popup (2026-08-30)"
- **A density that reaches OUTSIDE the card (2026-08-28)** — `FIRST_MARGIN`, `BETWEEN_MARGIN` and `LAST_MARGIN` are `--page-edge` and `--page-gap` now, so a chosen spacing must reach the page's own gaps, not only a card's padding. → `apps/hub/src/features/actors/HISTORY.md`, section "A density that reaches OUTSIDE the card (2026-08-28)"
- **Page interaction locks by default while editing (2026-09-02) — done** — The DOM boundary is `lockCanvasInteraction` (`presentation/canvas-interaction-lock.ts`), and it is the single enforcement point rather than a branch in every interactive leaf renderer. → `apps/hub/src/features/actors/HISTORY.md`, section "Page interaction locks by default while editing (2026-09-02) — done"
- **The five real defects, sorted by whose fault they were (2026-09-02)** — Credit a defect to the commit that introduced it, checked against `git log -S`, not to the task whose new coverage happened to expose it. → `apps/hub/src/features/actors/HISTORY.md`, section "The five real defects, sorted by whose fault they were (2026-09-02)"
- **The public routes have their own barrel (2026-09-03)** — `public.ts` holds only what the two signed-out routes render; nothing in it may pull the editor graph (react-hook-form, zod, `@dnd-kit`, Motion) into a public route's chunk. → `apps/hub/src/features/actors/HISTORY.md`, section "The public routes have their own barrel (2026-09-03)"
- **The compact builder menu, and one Add for one selection (2026-09-04)** — `addTargetFor(blocks, selection)` answers where the ONE Add control's next choice lands: the page root for nothing selected, a container's own path for a container, a leaf's PARENT for a leaf. → `apps/hub/src/features/actors/HISTORY.md`, section "The compact builder menu, and one Add for one selection (2026-09-04)"
- **The Properties panel replaces the recursive inspector (2026-09-04)** — Exactly two tabs, fixed per selection kind, never a variable number. → `apps/hub/src/features/actors/HISTORY.md`, section "The Properties panel replaces the recursive inspector (2026-09-04)"
- **Two stacking bugs and two selection bugs, closed by running the checkpoint's own e2e suite for real (2026-09-05)** — The Add picker's dialog resolved `fixed inset-0` against the toolbar, not the viewport. → `apps/hub/src/features/actors/HISTORY.md`, section "Two stacking bugs and two selection bugs, closed by running the checkpoint's own e2e suite for real (2026-09-05)"
- **A third, persistent Properties panel tab — visible, not yet draggable (2026-09-05) — Task 4 of 9, presentation** — `PropertiesPanel` renders unconditionally now. → `apps/hub/src/features/actors/HISTORY.md`, section "A third, persistent Properties panel tab — visible, not yet draggable (2026-09-05) — Task 4 of 9, presentation"
- **Palette thumbnails are real pointer drag sources onto the canvas (2026-09-05) — Task 5 of 9, presentation** — `onDragStart` branches on `palettePayload` first, and a palette id never falls through to the canvas-move logic below it. → `apps/hub/src/features/actors/HISTORY.md`, section "Palette thumbnails are real pointer drag sources onto the canvas (2026-09-05) — Task 5 of 9, presentation"
- **A palette drag also lifts by keyboard now (2026-09-05) — Task 7 of 9, presentation** — `paletteCoordinateAt` is the new function, and `paletteKeyboardTarget` is its own ref, parallel to `keyboardTarget` and cleared everywhere that one is. → `apps/hub/src/features/actors/HISTORY.md`, section "A palette drag also lifts by keyboard now (2026-09-05) — Task 7 of 9, presentation"
- **The page-source dock shares the Properties panel's own width token (2026-09-05)** — `--properties-panel-width` is the fix, declared once in `globals.css` as `min(36rem, 40vw)` rather than repeated as a literal in three files. → `apps/hub/src/features/actors/HISTORY.md`, section "The page-source dock shares the Properties panel's own width token (2026-09-05)"
- **The modal Add is retired; the palette is the only way in (2026-09-06) — Task 8 of 9** — There is no production-reachable control left that fills an existing empty place without growing its container — that capability is gone, never merely relocated. → `apps/hub/src/features/actors/HISTORY.md`, section "The modal Add is retired; the palette is the only way in (2026-09-06) — Task 8 of 9"
- **The closing sweep (2026-09-06) — Task 9 of 9** — `dragItemName(id)` checks `palettePayload(id)` first, naming the leaf kind or container mode — never assume `placeName` alone covers every id space a drag can carry. → `apps/hub/src/features/actors/HISTORY.md`, section "The closing sweep (2026-09-06) — Task 9 of 9"
